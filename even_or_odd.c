#include <stdio.h>
int main()
{
    int arr[5] = {10, 25, 30, 45, 50};

    for(int i=0; i<5; i++){
        if (arr[i]%2 == 0)
            printf("the no. is even %d\n", arr[i]);
        else
            printf("the no. is odd %d\n", arr[i]);
    }a
}
